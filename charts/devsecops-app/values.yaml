replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

resources: {}

probes:
  enabled: false

namespace: default
releaseName: devsecops-app
